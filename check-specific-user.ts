import "dotenv/config"; import connectDB from "./app/lib/db"; import { User } from "./app/models/User"; async function checkSpecificUser() { try { await connectDB(); const user = await User.findOne({ email: "student1@andrew.cmu.edu" }); console.log("User details:", JSON.stringify(user, null, 2)); process.exit(0); } catch (error) { console.error("Error:", error); process.exit(1); } } checkSpecificUser();
